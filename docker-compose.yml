version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PINECONE_API_KEY=${PINECONE_API_KEY}
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
    volumes:
      - ./:/app
    # host.docker.internal works on Mac/Windows natively.
    # On Linux, extra_hosts below maps it to the host gateway.
    extra_hosts:
      - "host.docker.internal:host-gateway"

  # Uncomment to run Ollama inside Docker (requires GPU for performance):
  # ollama:
  #   image: ollama/ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [gpu]

# volumes:
#   ollama_data:
